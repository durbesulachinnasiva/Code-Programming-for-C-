#include <iostream>
using namespace std;

class Student {
    string name;
    int age;
public:
    Student(string n, int a): name(n), age(a) {}
    void show() { cout << "Name: " << name << ", Age: " << age << endl; }
};

int main() {
    Student s("Ravi", 20);
    s.show();
    return 0;
}
